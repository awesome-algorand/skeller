---
import Page from "../layouts/Page.astro";
interface Props {
    error: unknown
}

const { error, ...rest } = Astro.props
---

<Page title="Error" description="Styff">
    {error instanceof Error ? error.message : 'Unknown error'}
    <pre>
        {JSON.stringify(rest, null, 2)}
    </pre>
</Page>
